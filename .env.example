# BigQuery Configuration
BIGQUERY_PROJECT=your-gcp-project-id
BIGQUERY_LOCATION=US

# Optional: Specific datasets to include (comma-separated)
# If not set, all datasets in the project will be available
# BIGQUERY_DATASETS=dataset1,dataset2

# Optional: Path to service account key file
# If not set, will use default Google Cloud credentials
# BIGQUERY_KEY_FILE=/path/to/service-account-key.json

# HTTP Server Configuration
# API key for authentication (required for ChatGPT integration)
MY_API_KEY=your-secret-api-key-here

# Server host and port (optional, defaults shown)
# HTTP_HOST=0.0.0.0
# HTTP_PORT=8000

# Optional: OpenAPI servers configuration for ChatGPT connectors
# Must be valid JSON array format
# OPENAPI_SERVERS=[{"url": "https://your-api-domain.com", "description": "Production"}]
